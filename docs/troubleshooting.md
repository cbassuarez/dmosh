# Troubleshooting

- If timelines look empty, confirm a source clip is imported.
- For export errors, ensure the ffmpeg wasm core has finished loading.
- On small screens, the editor is gated; switch to a desktop viewport.
