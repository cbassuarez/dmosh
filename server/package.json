{
  "name": "dmosh-server",
  "private": true,
  "type": "module",
  "description": "Heavy-mode reference server for dmosh (native ffmpeg + optional FFglitch).",
  "scripts": {
    "start": "tsx index.mjs",
    "check": "node --check index.mjs && node --check mosh.mjs"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "tsx": "^4.19.2"
  }
}
