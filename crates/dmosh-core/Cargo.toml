[package]
name = "dmosh-core"
version = "0.1.0"
edition = "2021"
description = "Native datamosh engine for dmosh (AVI frame surgery + ffmpeg orchestration)."

[dependencies]
serde = { version = "1", features = ["derive"] }
