import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    // put your Vitest config here, or keep it minimal for now
    // example:
    // globals: true,
    // environment: "jsdom",
  },
});