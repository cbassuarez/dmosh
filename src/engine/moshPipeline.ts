import type { MoshGraph } from '../mosh/moshModel'

export interface MoshCompiledPipeline {
  // Placeholder for future compiled pipeline state.
}

export function compileMoshPipelineFromGraphs(graphs: MoshGraph[]): MoshCompiledPipeline {
  void graphs
  return {}
}
